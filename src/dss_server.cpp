#include "dss_server.hpp"
#include "user.hpp"
#include <iostream>

void start_server(int port) {
    std::cout << "[SERVER] Listening on port " << port << std::endl;
    // Qui andrà la logica socket + OpenSSL
}
